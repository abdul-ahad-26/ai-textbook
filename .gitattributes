# Normalize line endings: store text as LF in the repo (CI builds on Linux).
* text=auto eol=lf

# Binary assets — never normalize.
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
